#!/usr/bin/env bash
set -e
set -o pipefail

freeze-requirements "$@" >/dev/null
json=$(update-index "$@" -r requirements.txt)

echo "$json" | generate-packages > python-modules/pypi-packages.nix
echo
echo "$json" | generate-versions
